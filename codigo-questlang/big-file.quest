// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend

// QuestLang extensive code - 500+ lines

if ( hero ) {
    // Section 1: Basic Movement (4 linhas)
    move_up
    move_left
    move_down
    move_right

    // Section 2: For Loop Training (10 iterações)
    for ( 0; 10; 1 ) {
        // Iteration block start
        move_up
        move_down
        if ( enemy ) {
            attack
            if ( enemy - 1 ) {
                jump
            } else {
                defend
            }
        } else {
            move_left
        }
        move_right
        // Additional commands for training
        move_up
        move_down
        // Iteration block end
    }

    // Section 3: While Loop Adventure
    while ( treasure ) {
        // Check for trap
        if ( trap ) {
            defend
            move_down
            if ( enemy ) {
                attack
                jump
            } else {
                move_left
            }
        } else {
            move_up
            move_right
        }
        jump
        attack
        defend
    }

    // Section 4: Nested For Loops
    for ( 0; 3; 1 ) {
        for ( 0; 3; 1 ) {
            move_up
            move_down
            if ( enemy ) {
                attack
            } else {
                move_left
            }
            move_right
        }
        jump
        move_up
    }

    // Section 5: Complex Conditional Structures
    if ( hero ) {
        if ( enemy ) {
            if ( treasure ) {
                attack
                jump
            } else {
                defend
            }
        } else {
            if ( trap ) {
                move_left
                move_down
            } else {
                move_up
                move_right
            }
        }
    } else {
        move_down
        defend
    }

    // Section 6: Repetitive Block 1 (repetido 10 vezes)
    for ( 0; 10; 1 ) {
        move_up
        move_down
        move_left
        move_right
        if ( enemy ) {
            attack
        } else {
            defend
        }
        jump
    }

    // Section 7: Repetitive Block 2 (repetição manual de blocos para acrescentar linhas)
    // Repeating a block of 20 commands, 10 vezes para adicionar muitas linhas.
    // Block repetition start 1
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    // Block repetition end 1

    // Block repetition start 2
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    jump
    move_down
    move_left
    move_right
    move_up
    defend
    attack
    // Block repetition end 2

    // Block repetition start 3
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    attack
    move_left
    move_right
    move_up
    move_down
    jump
    defend
    // Block repetition end 3

    // Block repetition start 4
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    defend
    move_up
    move_down
    move_left
    move_right
    attack
    jump
    // Block repetition end 4

    // Block repetition start 5
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    attack
    move_down
    move_left
    move_right
    move_up
    jump
    defend
    // Block repetition end 5

    // Block repetition start 6
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 6

    // Block repetition start 7
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 7

    // Block repetition start 8
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    jump
    move_down
    move_left
    move_right
    move_up
    attack
    defend
    // Block repetition end 8

    // Block repetition start 9
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    defend
    move_left
    move_right
    move_up
    move_down
    jump
    attack
    // Block repetition end 9

    // Block repetition start 10
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    attack
    move_up
    move_down
    move_left
    move_right
    defend
    jump
    // Block repetition end 10

    // Section 8: Additional Nested Structures
    if ( hero ) {
        for ( 0; 3; 1 ) {
            while ( enemy ) {
                if ( treasure ) {
                    jump
                    attack
                } else {
                    defend
                    move_up
                }
                move_down
            }
            move_left
        }
        if ( trap ) {
            move_right
        } else {
            move_up
        }
    } else {
        while ( treasure ) {
            move_down
            if ( enemy ) {
                attack
            } else {
                defend
            }
            jump
        }
    }

    // Section 9: More Nested Loops and Conditionals
    for ( 0; 4; 1 ) {
        if ( enemy ) {
            for ( 0; 2; 1 ) {
                move_up
                move_left
                if ( treasure ) {
                    attack
                    jump
                } else {
                    defend
                }
                move_down
            }
        } else {
            while ( trap ) {
                move_right
                jump
                if ( hero ) {
                    attack
                } else {
                    defend
                }
            }
        }
        move_up
        move_down
    }
} else {
    // Else block when hero is not present
    move_down
    move_left
    move_right
    attack
    defend
    jump

    for ( enemy; enemy; enemy + 1 ) {
        move_up
        if ( trap ) {
            defend
        } else {
            move_left
        }
        jump
        attack
        move_down
        if ( treasure ) {
            move_right
        } else {
            move_left
        }
        defend
    }

    while ( enemy ) {
        move_up
        move_down
        if ( treasure ) {
            attack
        } else {
            jump
        }
        move_right
        defend
    }

    for ( 0; 3; 1 ) {
        if ( trap ) {
            move_left
            attack
        } else {
            move_right
            defend
        }
        jump
        move_up
        move_down
    }
}

// Section 10: Final Marathon of Movements
for ( 0; 20; 1 ) {
    move_up
    move_down
    move_left
    move_right
    jump
    attack
    defend
    if ( hero ) {
        move_up
        if ( enemy ) {
            attack
        } else {
            defend
        }
    } else {
        if ( treasure ) {
            jump
        } else {
            move_down
        }
    }
}

// Section 11: Extra Repetitions to Reach 500+ Lines
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
move_up
move_down
move_left
move_right
jump
attack
defend
move_up
move_down
move_left
move_right
jump
attack
defend
